version: v1
environment: 
  name: cocoon
  definitions:
    - name: 'postgres'
      files:
        - docker-compose-postgres.yml
      commands:
        - container: postgres
          exec: postgres -V
    - name: 'redis'
      files:
        - docker-compose-redis.yml
  boot:
    - service1
    - service2